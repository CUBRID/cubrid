version: 2.1

# --------------------------------------------------
# Pipeline-level parameters (values passed from GitHub Actions)
# --------------------------------------------------
parameters:
  baseline:
    type: string
    default: "none"
  run_shell_test:
    type: boolean
    default: false

# --------------------------------------------------
# 1. Reusable commands
# --------------------------------------------------
commands:
  attach-and-run-tests:
    description: "Attach workspace and run test scripts"
    steps:
      - attach_workspace:
          at: .
      - run:
          name: Test
          shell: /bin/bash
          environment:
            _JAVA_OPTIONS: -Xmx1g
          command: |
            # Limit core files
            ulimit -c 1

            # Check out code (uses custom entrypoint script)
            /entrypoint.sh checkout

            # Handle specific cases for TEST_SUITE
            if [ "$TEST_SUITE" = "none" ]; then
              echo "TEST_SUITE is none"
              exit 1
            elif [ "$TEST_SUITE" = "plcsql" ]; then
              # Hack: run plcsql test via sql suite
              TEST_SUITE="sql"
              glob_path="cubrid-testcases/sql/{_05_plcsql,_35_fig_cake/plcsql}"
            elif [ "$TEST_SUITE" = "sql" ]; then
              rm -rf cubrid-testcases/sql/_05_plcsql
              rm -rf cubrid-testcases/sql/_35_fig_cake/plcsql
              glob_path="cubrid-testcases/$TEST_SUITE/_*"
            elif [ "$TEST_SUITE" = "shell" ]; then
              # Handle shell test case location
              rm -rf cubrid-testcases-private-ex/shell/_06_issues
              rm -rf cubrid-testcases-private-ex/shell/_25_unstable
              glob_path="cubrid-testcases-private-ex/shell/_*"
            else
              glob_path="cubrid-testcases/$TEST_SUITE/_*"
            fi

            # CircleCI test splitting
            circleci tests glob "$glob_path" | circleci tests split | tee tc.list
            find "$glob_path" -maxdepth 0 -type d -print0 | grep -vzZ -f tc.list | xargs -0 rm -rf

            # Run the tests
            /entrypoint.sh test

      - run:
          name: Collect Logs
          command: |
            mkdir -p /tmp/logs
            mv -f CUBRID/log /tmp/logs/cubrid_log || true
            mv -f cubrid-testtools/CTP/sql/log /tmp/logs/ctp_log || true
            mv -f /home/shell/cubrid-testtools/CTP/result/shell/current_runtime_logs /tmp/logs/ctp_log || true
            find /home -name "core.*" | xargs -I{} mv -f {} /tmp/logs
          when: on_fail

      - store_test_results:
          path: /tmp/tests

      - store_artifacts:
          path: /tmp/logs

  collect-build-logs:
    description: "Collect build logs and store them"
    steps:
      - run:
          name: Store Build Logs
          command: |
            mkdir -p /tmp/build_logs
            mv -f build.log /tmp/build_logs || true
      - run:
          name: Store Commits
          command: |
            mkdir -p /tmp/build_logs
            git log | head -n 1000 > commits.log || true
            mv -f commits.log /tmp/build_logs
      - store_artifacts:
          path: /tmp/build_logs

# --------------------------------------------------
# 2. Reusable executors
# --------------------------------------------------
executors:
  cubrid-default:
    docker:
      - image: cubridci/cubridci:develop
    working_directory: /home

  ctp_shell:
    docker:
      - image: twkang3219/ctp_image_shell:latest
    working_directory: /home

# --------------------------------------------------
# 3. Jobs
# --------------------------------------------------
jobs:
  build:
    executor: cubrid-default
    resource_class: xlarge
    environment:
      MAKEFLAGS: -j 8
    steps:
      - checkout
      - run:
          name: Submodules
          command: |
            git submodule sync
            git submodule update --init
      - run:
          name: Build
          command: |
            scl enable devtoolset-8 -- /entrypoint.sh build
      - collect-build-logs
      - persist_to_workspace:
          root: .
          paths:
            - CUBRID

  # 단일 test_job 정의: job-level 파라미터 test_suite 사용
  test_job:
    parameters:
      e:
        type: executor
        default: cubrid-default
      test_suite:
        type: string
        default: "none"
      parallel_num:
        type: integer
        default: 1
    executor: << parameters.e >>
    environment:
      BASELINE: << pipeline.parameters.baseline >>
      RUN_SHELL_TEST: << pipeline.parameters.run_shell_test >>
      TEST_SUITE: << parameters.test_suite >>
      GITHUB_TOKEN: $GHI_TOKEN
    resource_class: medium
    parallelism: << parameters.parallel_num >>
    steps:
      - run:
          name: Setup Environment and Debug Info
          command: |
            echo "TEST_SUITE: ${TEST_SUITE}"    # Debug: print TEST_SUITE
            echo "BASELINE: ${BASELINE}"        # Debug: print BASELINE
            echo "RUN_SHELL_TEST: ${RUN_SHELL_TEST}"  # Debug: print RUN_SHELL_TEST
            echo "executor: ${executor}"
            echo "parallel_num: ${parallel_num}"
            
            if [ "${RUN_SHELL_TEST}" == "true" ]; then
              echo "Setup shell test worker"
              /entrypoint.sh worker
            fi
      - attach-and-run-tests

  build-windows:
    machine:
      image: "windows-server-2019"
      resource_class: windows.large
      shell: powershell.exe -ExecutionPolicy Bypass
    environment:
      CMAKE_GENERATOR: "Visual Studio 15 2017"
      CMAKE_BUILD_PARALLEL_LEVEL: 10
      WITH_CCI: true
    steps:
      - checkout
      - run:
          name: Submodules
          command: |
            git submodule update --init --recursive
            git rm -f cubridmanager
      - run:
          name: Install dependencies
          command: |
            choco upgrade chocolatey -y
            choco install openjdk8redhatbuild --no-progress -y
            choco install cmake --version=3.26.3 --installargs 'ADD_CMAKE_TO_PATH=System' --no-progress -y
            choco install winflexbison wixtoolset --no-progress -y
            choco install ant --ignore-dependencies --no-progress -y
            choco install visualstudio2017buildtools --package-parameters "--add Microsoft.VisualStudio.Workload.VCTools --add Microsoft.VisualStudio.Component.VC.ATLMFC --includeRecommended" --no-progress -y
      - run:
          name: CMake Configure
          command: |
            cmake -E make_directory build
            cmake -E chdir build cmake -G $env:CMAKE_GENERATOR -A x64 -DWITH_CCI=$env:WITH_CCI -DCMAKE_BUILD_TYPE=RelWithDebInfo ..
      - run:
          name: CMake Build
          command: |
            cmake --build build --config RelWithDebInfo --parallel $env:CMAKE_BUILD_PARALLEL_LEVEL

# --------------------------------------------------
# 4. Workflows
# --------------------------------------------------
workflows:
  build_test:
    jobs:
#      - build
#       - test_job:
#           name: test_medium
#  #         requires: [build]
#           test_suite: "medium"

#       - test_job:
#           name: test_sql
#           requires: [build]
#           test_suite: "sql"
#           parallel_num: 8

#       - test_job:
#           name: test_plcsql
#           requires: [build]
#           test_suite: "plcsql"

      - test_job:
          name: test_shell
          # requires: [build]
          test_suite: "shell"
          e: ctp_shell
          parallel_num: 1

      # - build-windows:
      #     requires: [build]