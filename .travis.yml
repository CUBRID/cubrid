sudo: required

services:
  - docker

language: c

compiler:
  - gcc

os:
- linux

script:
  - docker run -v./cubrid:/home/cubrid -v./CUBRID:/home/CUBRID -e MAKEFLAGS='-j 2' cubridci/build
  - ls
  - ls -al CUBRID
